# phishing-email-detector
Phishing Email Detection ML pipeline using TF-IDF and custom features (URL + urgency keywords) with Logistic Regression. Built with Scikit-learn Pipeline, supports DataFrame/list inputs, uses sparse optimization, and is production-ready with joblib serialization for deployment.
